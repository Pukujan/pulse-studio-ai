# 📦 How to Export Pulse Studio to Your Local Machine

Follow these steps to get this prototype running in VS Code on your computer.

---

## 🎯 Quick Start (3 Steps)

### Step 1: Download All Files

Download this entire project folder with all files and subfolders:

```
pulse-studio/
├── components/       (entire folder)
├── data/            (entire folder)
├── styles/          (entire folder)
├── guidelines/      (entire folder)
├── src/             (entire folder)
├── App.tsx
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.node.json
├── index.html
├── .gitignore
├── .eslintrc.cjs
├── SETUP.md
├── README.md
└── Attributions.md
```

**Make sure you get ALL files and folders!**

### Step 2: Open Terminal in the Folder

**Windows:**
- Open the folder in File Explorer
- Hold `Shift` + Right-click → "Open PowerShell window here"
- Or just type `cmd` in the address bar

**Mac/Linux:**
- Open Terminal
- Navigate: `cd /path/to/pulse-studio`

### Step 3: Install & Run

```bash
# Install all dependencies (one-time setup)
npm install

# Start the development server
npm run dev
```

Then open `http://localhost:5173/` in your browser.

---

## 🔍 Detailed Instructions

See **SETUP.md** for:
- Prerequisites checklist
- Troubleshooting guide
- Project structure overview
- Available npm commands
- Development tips

---

## ✅ Verification Checklist

Before running `npm install`, make sure you have:

- [ ] All files from the list above
- [ ] The complete `/components/` folder (with all subfolders)
- [ ] The complete `/data/` folder (with all .ts files)
- [ ] The `/styles/globals.css` file
- [ ] `package.json` file (contains all dependencies)
- [ ] `index.html` file
- [ ] `vite.config.ts` file

---

## 🚨 Common Mistakes to Avoid

❌ **Don't** download files individually - download the entire folder structure  
❌ **Don't** skip the `npm install` step  
❌ **Don't** modify file paths or folder names  
✅ **Do** keep the exact folder structure as-is  
✅ **Do** have Node.js v18+ installed first  

---

## 💾 File Download Methods

### Method 1: Download as ZIP (Easiest)

If you have a GitHub repo or similar:
1. Click "Code" → "Download ZIP"
2. Extract the ZIP file
3. Navigate to the extracted folder
4. Run `npm install` and `npm run dev`

### Method 2: Clone with Git

```bash
git clone <repository-url>
cd pulse-studio
npm install
npm run dev
```

### Method 3: Manual Download (From Figma Make)

If you're viewing this in Figma Make:
1. Use the export/download feature to get all files
2. Ensure folder structure is preserved
3. Follow Step 2 and 3 above

---

## 🎉 What You'll Get

A fully functional prototype with:

✅ Dark, modern UI with gradient effects  
✅ Interactive AI chat panel (mock conversation)  
✅ Logo upload functionality  
✅ 12 customizable post templates  
✅ 4 background feels with curated images  
✅ Platform previews (Instagram, TikTok, Facebook)  
✅ Template switching with flash animations  
✅ Responsive design  
✅ Clean, maintainable code architecture  

---

**Questions?** Check SETUP.md for detailed troubleshooting! 🚀
